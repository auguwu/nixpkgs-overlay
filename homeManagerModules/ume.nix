# A `home-manager` module to write a configuration file in `~/.config/Noel/ume.toml` and
# allow overwriting keyboard shortcuts (for GNOME Shell / KDE Plasma) to `<Ctrl>Print` (or
# whatever in `programs.ume.keyboardShortcuts.shortcut`)
{}
